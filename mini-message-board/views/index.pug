extends layout

block content
  h1 #{ title }
  a(href='/new') I've got something to say!

  ul
    each message in messages
      li
        p #{ message.text }
        p #{ message.user }
        p #{ message.added }